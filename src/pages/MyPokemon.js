import React from 'react';
import { Layout } from '../layout';

const MyPokemon = (props) => {

    return (
        <Layout>
            this is MyPokemon page!
        </Layout>
    )
}

export default MyPokemon;